apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.proxy.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  ports:
  - name: http
    port: 9999
    nodePort: 31222
    protocol: TCP 
  selector:
    app: {{ .Values.proxy.name }}
